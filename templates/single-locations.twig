{% extends 'templates/_document.twig' %}

{% block content %}
  <article>
    {{ renderComponent('BlockLocationContent', {post: post}) }}
    {{ renderComponent('BlockPostContent', {post: post}) }}
  </article>
{% endblock %}
